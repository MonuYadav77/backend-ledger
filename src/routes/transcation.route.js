const {Router} = require('express');

const transcationRoutes = Router();

/**
 * -POST /api/transcation
 * -Create a new transcation
 */

transcationRoutes.post("/");
module.exports = transcationRoutes;
